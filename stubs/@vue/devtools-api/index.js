// Minimal stub of @vue/devtools-api used so devtools code is a no-op in production/dev.
// Provide the common API surface Pinia expects so runtime doesn't fail.

export function setupDevtoolsPlugin() {
  // no-op
}
